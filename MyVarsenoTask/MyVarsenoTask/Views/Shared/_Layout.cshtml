<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - My Varseno Task </title>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")
</head>
<body>
    @if (User.Identity.IsAuthenticated)
    {
        <div class="navbar navbar-inverse navbar-fixed-top">
            <div class="container">
                <div class="navbar-header">
                    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </button>
@*                    @Html.ActionLink("Application name", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })
*@                </div>
                <div class="navbar-collapse collapse">
                    <ul class="nav navbar-nav">
                        <li>@Html.ActionLink("Get List", "Index", "TODOLIST")</li>
                        <li>@Html.ActionLink("Create", "Create", "TODOLIST")</li>
                        @*                                            <li class="active">@Html.ActionLink("User Profile", "Edit", "Account")</li>
                        *@@**@                                           @* @if (Session["UserId"] != null)
                        {
        <li><a href="@Url.Action("Edit", "Account", new { id = @Session["UserId"] })"></a></li>

                        }*@
                        @if (Session["UserId"] != null)
                        {
                            <li><a href="@Url.Action("Edit", "Account", new { id = @Session["UserId"] })">Edit Profile</a></li>

                        }

                        @*                         <li>@Html.ActionLink("Edit", "Edit", "Index")</li>
                        *@
                        @if (User.Identity.IsAuthenticated)
                        {
                            <li><a>Dear @User.Identity.Name</a></li>
                            <li>@Html.ActionLink("Logout", "Logout", "Account")</li>

                        }
                        @* <li>Login</li>
        <li>@Html.ActionLink("Home", "Index", "Home")</li>
           <li>@Html.ActionLink("About", "About", "Home")</li>
           <li>@Html.ActionLink("Contact", "Contact", "Home")</li>*@
                    </ul>
                </div>
            </div>
        </div>
    }
        <div class="container body-content">
            @RenderBody()
           @* @if (User.Identity.IsAuthenticated)
            {
                <footer>
                    <p>&copy; @DateTime.Now.Year - My ASP.NET Application</p>
                </footer>
            }*@

            @* <hr />
                <footer>
                    <p>&copy; @DateTime.Now.Year - My ASP.NET Application</p>
                </footer>*@
        </div>

        @Scripts.Render("~/bundles/jquery")
        @Scripts.Render("~/bundles/bootstrap")
        @RenderSection("scripts", required: false)
    </body>
</html>
